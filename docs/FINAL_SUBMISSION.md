# Final Devpost submission field pack

This is the locked submission copy. Replace only the three URL placeholders after publication; do not alter technical claims without rerunning `./scripts/verify.sh` and updating `docs/RELEASE_EVIDENCE.md`.

## Project name

ContextLoop

## Tagline

Close the schema-change loop with DataHub-grounded impact, action, and memory.

## Challenge

Agents That Do Real Work

## Project URL

`<PUBLIC_PROJECT_OR_REPOSITORY_URL>`

## Public code repository URL

`<PUBLIC_REPOSITORY_URL>`

## Public demo video URL

`<PUBLIC_YOUTUBE_VIMEO_OR_YOUKU_URL>`

## Built with

DataHub OSS, DataHub Agent Context Kit, Python, FastAPI, Pydantic, React, TypeScript, Vite, SVG, Codex CLI, and ChatGPT OAuth.

## Submission description

Use the text in `docs/DEVPOST_DESCRIPTION.md` without adding claims about hosted availability, OpenAI API usage, autonomous mutation, or an upstream open-source contribution.

## Testing instructions

Use the Quick start and Free judge mode sections in `README.md`. The repository itself is the free test build. Keep it public and unchanged through August 31, 2026 at 5:00 PM EDT.

## Video constraints

Record `docs/DEMO_SCRIPT.md` against the real OAuth path. The final cut must be under three minutes, public, show the working desktop app, contain no credentials or local paths, add no third-party logos or copyrighted music, and match the recorded live run and committed product claims. Prior-memory counts may differ between runs because they are retrieved live; the video must display and narrate its own grounded count without substituting the sample run's value.

## License

Apache License 2.0. Confirm that GitHub detects `LICENSE` and displays `Apache-2.0` in the repository About area.

## Claims that are intentionally not made

- No hosted SaaS deployment is claimed.
- No OpenAI Platform API or API-key backend is claimed.
- Free judge mode is disclosed as deterministic and makes no model call.
- No DataHub upstream contribution or bonus criterion is claimed unless a public contribution URL is added before submission.
